{
  "name": "es-sink-test",
  "config": {
    "connector.class": "io.confluent.connect.elasticsearch.ElasticsearchSinkConnector",
    "tasks.max": "1",

    "topics": "test_prefix.darbsciu_rankuciu_klubas.events",

    "connection.url": "https://es01:9200",
    "connection.username": "elastic",
    "connection.password": "changeme",

    "key.ignore": "true",
    "schema.ignore": "true",

    "elastic.security.protocol": "SSL",

    "elastic.https.ssl.truststore.location": "/tmp/truststore.jks",
    "elastic.https.ssl.truststore.password": "changeit",

    "elastic.https.ssl.keystore.location": "",
    "elastic.https.ssl.keystore.password": "",
    "elastic.https.ssl.key.password": "",

    "errors.tolerance": "all",
    "errors.log.enable": "true"
  }
}
